{{ log_dir }}/*.log {
    daily
    missingok
    rotate {{ log_max_backups }}
    compress
    delaycompress
    notifempty
    create 0644 {{ deploy_user }} {{ deploy_group }}
    sharedscripts
    postrotate
        /usr/bin/systemctl reload smtp-server > /dev/null 2>&1 || true
    endscript
}
